name: Create Test Issue

on:
  workflow_dispatch:
  issues:
    types: [labeled]

permissions:
  issues: write
  contents: read

jobs:
  create-test-issue:
    runs-on: ubuntu-latest
    
    steps:
      - name: Create On Demand Validation Test Issue 
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issue = context.payload.issue;
            const label = context.payload.label;
            
            if (label.name !== 'need-ondemand-validation') {
              return;
            }
            
            const tokenMatch = issue.title.match(/Add validator for\s+(.+)$/i);
            const tokenName = tokenMatch ? tokenMatch[1] : 'UNKNOWN_TOKEN';
            
            const newIssue = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Enable on-demand validation for ${tokenName}`,
              body: `## On-Demand Validation Task

              **Original Issue:** #${issue.number}
              **Token:** ${tokenName}

              ### Implementation Steps:
              - [ ] Edit \`config.go\` file to add:
                - [ ] \`WithOnDemandChecksSupportedForTokenTypes("${tokenName}")\`
                - [ ] \`WithValidityChecksSupportedForTokenTypes("${tokenName}")\`
                - [ ] Remove \`DisableValidityUpdatesForTokenTypes("${tokenName}")\` if present

              ### Files to Update:
              - \`ts/partnervalidation/validators/config/config.go\`

              **Auto-created from:** ${issue.title}`,
                            labels: ['auto-created', 'on-demand-validation']
                          });
                          
                        